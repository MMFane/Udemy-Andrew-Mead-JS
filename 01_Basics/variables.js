// 1. Can't define var more than once
// 2. Var name rules :
//      can only start with letters, $, or _
//      only special chars allowed are $ and _
//      can't be reserved keywords

let petName = "Bones";
console.log(petName);